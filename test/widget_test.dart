// test/widget_test.dart

import 'package:flutter_test/flutter_test.dart';

import 'package:flutter_inventario_app/main.dart';

void main() {
  test('FlutterInventarioApp can be constructed', () {
    expect(const FlutterInventarioApp(), isA<FlutterInventarioApp>());
  });
}